{
  "name": "@fand/react-infinite-scroll-container-example",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "NODE_PATH=node_modules browserify index.js -o bundle.js -t babelify",
    "watch": "NODE_PATH=node_modules watchify index.js -o bundle.js -t babelify",
    "test": "echo \"Error: no test specified\" && exit 1",
    "link": "cd .. && npm link && cd example && npm link @fand/react-infinite-scroll-container",
    "serve": "budo -o",
    "start": "npm-run-all -p watch serve"
  },
  "author": "fand",
  "license": "MIT",
  "dependencies": {
    "@fand/react-infinite-scroll-container-test": "*"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "budo": "^8.0.4",
    "npm-run-all": "^1.5.1",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  }
}
